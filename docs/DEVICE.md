# Dispositivo

Estado: `NOT CONNECTED / UNVALIDATED`.

O modelo, firmware, hardware, serial, bateria, serviços e characteristics só serão preenchidos após descoberta real. A ausência de um campo no dashboard deve aparecer como `Not available yet`, nunca como zero.
