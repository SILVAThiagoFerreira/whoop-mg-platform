# Jarvis Architecture

Jarvis Core orquestrará Data Engine, Body Model, memória, ferramentas, LLM, alertas e API. Cada conclusão importante deverá carregar `claim`, `type`, `sample_size`, `method`, `confidence`, `evidence` e `limitations`.
